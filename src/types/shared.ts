export type NavLink = {
  label: string;
  href: string;
};

export type Testimonial = {
  id: string;
  name: string;
  location: string;
  quote: string;
};

export type FAQ = {
  id: string;
  question: string;
  answer: string;
};
