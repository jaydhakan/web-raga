# Comma-separated hostnames to allow in the Vite dev server.
# Useful when accessing via a tunnel or custom domain during development.
# Example: VITE_ALLOWED_HOSTS=raga.vectanix.com,preview.vectanix.com
VITE_ALLOWED_HOSTS=
